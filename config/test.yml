authBeEndpoint: 'http://localhost:3001/api/graphql'

dynamodb:
  schema:
    workflow: 'workflow_test'
    workflowOrganization: 'workflow_organizations_test'

jwt:
  secret: 'topSecretTest51'

files:
  dir: './test/uploads/'
